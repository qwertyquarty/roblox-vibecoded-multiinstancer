# roblox-vibecoded-multiinstancer
works fine, has a systray to close it on click

compile either with `cl tray.c user32.lib shell32.lib` or `gcc tray.c -mwindows -luser32 -lshell32` depending on your preference
